databaseChangeLog:
- changeSet:
    id: monitor-init-trigger-1
    author: limit
#    changes:
#        - createProcedure:
#            procedureName: ${db_prefix}monitor_trg
#            relativeToChangelogFile: true
#            procedureBody: |
#                CREATE OR REPLACE TRIGGER ${db_prefix}monitor_trg
#                    BEFORE INSERT ON ${db_prefix}monitor
#                    FOR EACH ROW
#                BEGIN
#                    IF :NEW.id IS NULL THEN
#                        SELECT ${db_prefix}monitor_seq.NEXTVAL INTO :NEW.id FROM DUAL;
#                    END IF;
#                END;
