databaseChangeLog:
- changeSet:
    id: acl-init-index-1
    author: limit
    changes:
      - createIndex:
          indexName: ${db_prefix}acl_entry_map_idx_subject
          tableName: ${db_prefix}acl_entry_map
          columns:
            - column:
                name: subject_type
            - column:
                name: subject_value
      - createIndex:
          indexName: ${db_prefix}acl_entry_map_idx_resource
          tableName: ${db_prefix}acl_entry_map
          columns:
            - column:
                name: resource_type
            - column:
                name: resource_id
