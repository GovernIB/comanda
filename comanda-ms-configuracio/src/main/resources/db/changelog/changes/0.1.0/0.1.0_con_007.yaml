databaseChangeLog:
  - changeSet:
      id: conf-change-2473-1
      author: Limit
      changes:
        - modifyDataType:
            tableName: ${db_prefix}integracio
            columnName: nom
            newDataType: varchar(255 ${varcharUnit})
        - modifyDataType:
            tableName: ${db_prefix}app_subsistema
            columnName: codi
            newDataType: varchar(64 ${varcharUnit})
        - modifyDataType:
            tableName: ${db_prefix}app_subsistema
            columnName: nom
            newDataType: varchar(255 ${varcharUnit})
        - modifyDataType:
            tableName: ${db_prefix}app_context
            columnName: nom
            newDataType: varchar(255 ${varcharUnit})