databaseChangeLog:
- changeSet:
    id: conf-change-codi-100-1
    author: Limit
    comment: Ampliar mida de codis
    changes:
      - modifyDataType:
          tableName: ${db_prefix}integracio
          columnName: codi
          newDataType: varchar(100 ${varcharUnit})
      - modifyDataType:
          tableName: ${db_prefix}app_subsistema
          columnName: codi
          newDataType: varchar(100 ${varcharUnit})
      - modifyDataType:
          tableName: ${db_prefix}app_context
          columnName: codi
          newDataType: varchar(100 ${varcharUnit})
