package es.caib.comanda.estadistica.logic.intf.model.consulta;

/**
 * Enum que representa els diferents nivells d'agrupació temporal que es poden aplicar.
 * Permet definir agrupacions basades en diferents períodes com ara any, trimestre, mes, setmana o dia.
 *
 * @author Límit Tecnologies
 */
public enum NivellAgrupacio {
    ANY,
    TRIMESTRE,
    MES,
    SETMANA,
    DIA
}
