package es.caib.comanda.estadistica.logic.intf.service;

import es.caib.comanda.estadistica.logic.intf.model.estadistiques.IndicadorFormulaTerme;
import es.caib.comanda.ms.logic.intf.service.MutableResourceService;

/**
 * Interfície que defineix els serveis per gestionar recursos de tipus "IndicadorFormulaTerme".
 *
 * @author Límit Tecnologies
 */
public interface IndicadorFormulaTermeService extends MutableResourceService<IndicadorFormulaTerme, Long> {
}
