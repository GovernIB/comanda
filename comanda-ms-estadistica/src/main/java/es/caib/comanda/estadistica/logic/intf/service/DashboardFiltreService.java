package es.caib.comanda.estadistica.logic.intf.service;

import es.caib.comanda.estadistica.logic.intf.model.dashboard.DashboardFiltre;
import es.caib.comanda.ms.logic.intf.service.MutableResourceService;

/**
 * Servei de gestió i consulta per filtres de capçalera de quadres de comandament (Dashboard).
 *
 * @author Límit Tecnologies
 */
public interface DashboardFiltreService extends MutableResourceService<DashboardFiltre, Long> {

}
